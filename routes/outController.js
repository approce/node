var log    = require('winston');

function connect(nodeInfo) {
}

function update(number) {
}

function send(request) {
}

module.exports = {
    connect: connect,
    update : update,
    send   : send
};
